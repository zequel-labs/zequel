// Stub for elkjs's optional web-worker dependency.
// elkjs conditionally requires 'web-worker' for Node.js worker threads,
// but falls back to the browser's built-in Worker API when unavailable.
export default undefined
